package com.example.eatelo;

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;

public class SignupActivity extends AppCompatActivity {
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        // Set the layout for this activity
        setContentView(R.layout.signup); // Make sure you have the corresponding XML layout file

        // Initialize UI elements here (buttons, text fields, etc.)
        // For example, you might want to set up a sign-up form with edit text and a submit button
    }
}
